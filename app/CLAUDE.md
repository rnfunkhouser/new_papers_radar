# Claude Code instructions

Read `AGENTS.md` in this directory and follow it — it explains what this app needs from an
LLM provider, how to configure `llm_api.json`, how to verify the connection, and the rules
(standard library only; never commit secrets; never alter ranking logic to fix a connection
problem).
